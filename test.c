#include "stdio.h"
#include "stdlib.h"
#include "string.h"
#include "math.h"
#include "time.h"
#include "ctype.h"
#include "stdbool.h"
#include "limits.h"
include
#include "float.h"
#include "assert.h"
int main()
{
    int a = 0;
    int b = 0;
    int c = 0;

    // 计算a的阶乘
    for (a = 1; a <= 10; a++)
    {
        for (b = 1; b <= a; b++)
        {
            printf("%d", b);
        }
        printf("\n");
    }
    // spi 波特率
    // spi 数据位
    // spi 校验位

    //spi 参数设置
    // spi 数据位
    // spi 校验位
    // spi 停止位
    // spi 模式
    // spi 设备
    // spi 设备号
    // spi 设备名称


    // spi 设备号
    